import { LoginForm } from '@/components/auth/login-form';

export const metadata = {
  title: 'Login | Spare Parts Portal',
  description: 'Login to the Spare Parts Inventory & Sales Management Portal',
};

export default function LoginPage() {
  return <LoginForm />;
}
